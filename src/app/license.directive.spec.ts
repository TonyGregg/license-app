import { LicenseDirective } from './license.directive';

describe('LicenseDirective', () => {
  it('should create an instance', () => {
    const directive = new LicenseDirective();
    expect(directive).toBeTruthy();
  });
});
