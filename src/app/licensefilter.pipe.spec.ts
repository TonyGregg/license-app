import { LicensefilterPipe } from './licensefilter.pipe';

describe('LicensefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LicensefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
